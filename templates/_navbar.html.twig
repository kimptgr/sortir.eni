<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">

        <!-- Logo / Titre du site -->
        <a class="navbar-brand" href="{{ path('main') }}"><h1>sortir.com</h1></a>


        <!-- Bouton pour le mode mobile -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>


        <!-- Liens de navigation -->
        <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
            <ul class="navbar-nav mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('main') }}">Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_trip_index') }}">List des événements</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_trip_new') }}">Créer un événement</a>
                </li>
            </ul>
            <ul class="navbar-nav mb-2 mb-lg-0">
                {% if app.user %}


                    <li class="nav-item">
                         <a class="nav-link" href="{{ path('app_profile') }}">Profil</a>
                    </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}">Se déconnecter</a>
                </li>
                {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_login') }}">Se connecter</a>
                </li>

                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_register') }}">Créer compte</a>
                    </li>

                {% endif %}
                <li class="nav-item">
                    {# <a class="nav-link" href="{{ path('') }}">Machin</a> #}
                </li>
                <li class="nav-item">
                    {# <a class="nav-link" href="{{ path('') }}">Machin</a> #}
                </li>
            </ul>
        </div>
    </div>
</nav>
