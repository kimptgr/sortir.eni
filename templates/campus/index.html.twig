{% extends 'base.html.twig' %}

{% block title %}Gestion campus | {{ parent() }}{% endblock %}

{% block body %}
    <h1>Gérer les campus</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Campus</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for campus in campuses %}
            <tr>
                <td>{{ campus.name }}</td>
                <td>
                    <a href="{{ path('app_campus_edit', {'id': campus.id}) }}">Modifier</a>
                    - {{ include('campus/_delete_form.html.twig') }}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        <tr>
            {{ form_start(form) }}
            <td>{{ form_widget(form) }}</td>
            <td><button class="btn">{{ button_label|default('Enregistrer') }}</button></td>
            {{ form_end(form) }}
        </tr>
        </tbody>
    </table>

    <a href="{{ path('app_campus_new') }}">Create new</a>
{% endblock %}
